Subtipos
BEGIN
    ##ErrorSintactico
    ##typedef enterito  := longint[-10,10];
    ##ErrorSintactico
    ##typedef flotantito := single[-1.2, 3.5];
    ##ErrorSintactico
    ##typedef := longint[-10,10];

    typedef enterito  := longint{-10,10};

END