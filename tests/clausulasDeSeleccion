clausulaSeleccion
BEGIN   

    ## Error falta parentesis en la condicion
    if (x > 0 then outf([hola]);end_if;

    ## Error falta parentesis en la condicion
    if x > 0) then outf([hola]);end_if;

    ## Error falta parentesis en la condicion
    if x > 0 then outf([hola]);end_if;

    ## Error falta parentesis en la condicion
    longint fun asdas(longint x) begin
        if x > 0 then ret (x + 4);end_if;
    end;
    ## Error falta bloque de sentencias valida
    if (x > 0) then ;end_if;

    ## Error falta bloque de sentencias valida
    if (x > 0) then ret (x + 4);end_if;


    ## Error falta bloque de sentencias valida ##PROBLEMA CON EL NUMERO DE LINEA
    if (x > 0) then longint declaracion;
    end_if
    ;

    ##bien
    if (x > 0) then int := 2; end_if;

    ##bien
    longint fun asdas(longint x) begin
        if (x > 0) then ret (x + 4);end_if;
    end;


end