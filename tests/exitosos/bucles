bucles
BEGIN
    repeat 
        begin
            repeat y := 10; while (y < 0);
            x:=2; 
            goto ola@;
        end 
    while (x > 0);

    ola:

    typedef triple<longint> triplete;
    triplete [2] := 2;
end