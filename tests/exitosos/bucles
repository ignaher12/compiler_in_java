bucles
BEGIN
	longint y;
	longint x;
    repeat 
        begin
            repeat y := 10; while (y < 0);
            x:=2; 
            goto ola@;
        end 
    while (x > 0);

    ola:

    typedef triple<longint> triplete;
	triplete a;
    a[2] := 2;
	outf(a[2]);
end