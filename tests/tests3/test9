conversiones
BEGIN
    longint a;
    longint b;
    a := 16;
    b := 20;
    longint FUN funcion(single x)
    begin
        longint f;
        f := 1;
        outf([DENTRO DE FUNCION]);
        outf(x);
        ret(f);
    end;
    a:= funcion(single a);

    REPEAT begin
        a:= a + 1;
        outf(a);
    end 
    WHILE (a < b);
end
